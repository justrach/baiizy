# PostgreSQL connection string
# Example: postgresql://USER:PASSWORD@HOST:5432/baiizy
DATABASE_URL=

# GrabMaps (optional — app works with mock data without this)
GRAB_MAPS_API_KEY=
GRAB_MAPS_MCP_URL=https://maps.grab.com/api/v1/mcp

# better-auth — generate: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
